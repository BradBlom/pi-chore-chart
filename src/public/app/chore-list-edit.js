document.addEventListener('DOMContentLoaded', () => {
  const container = document.getElementById('chore-list-edit-content');
  if (container) {
    container.innerHTML = '<h1>Edit Chore List</h1><p>Chore list editing will appear here.</p>';
  }
});