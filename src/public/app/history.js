document.addEventListener('DOMContentLoaded', () => {
  const container = document.getElementById('history-content');
  if (container) {
    container.innerHTML = '<h1>History</h1><p>Chore history will appear here.</p>';
  }
});